# Dependencies
node_modules/

# Build artifacts
dist/
.vite/
*.tsbuildinfo
# tsc -b emits these from vite.config.ts via the project-reference build
vite.config.js
vite.config.d.ts

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Env / secrets
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Test coverage
coverage/

# Vite cache
.cache/

# Raw source-document drops (large offline audit copies; not redistributed —
# manifest.json + extracted article-level citations are what we commit).
data/raw/

# Claude Code local state (launch.json is tracked; these are per-machine)
.claude/settings.local.json
.claude/worktrees/
